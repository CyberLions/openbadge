{
  "name": "OpenBadge Platform",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:22-bookworm",
  "workspaceFolder": "/workspaces/openbadge",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/openbadge,type=bind,consistency=cached",
  "initializeCommand": "cd ${localWorkspaceFolder} && docker compose up -d",
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "forwardPorts": [3000, 5173, 5432, 8025],
  "portsAttributes": {
    "3000": { "label": "Backend API", "onAutoForward": "notify" },
    "5173": { "label": "Vue Frontend", "onAutoForward": "openBrowser" },
    "5432": { "label": "PostgreSQL", "onAutoForward": "silent" },
    "8025": { "label": "MailHog UI", "onAutoForward": "notify" }
  },
  "runArgs": ["--network=host"],
  "containerEnv": {
    "DATABASE_URL": "postgresql://openbadge:openbadge_dev@localhost:5432/openbadge",
    "SMTP_HOST": "localhost",
    "SMTP_PORT": "1025",
    "SMTP_SECURE": "false",
    "APP_URL": "http://localhost:3000",
    "FRONTEND_URL": "http://localhost:5173",
    "NODE_ENV": "development"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "Vue.volar",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "Prisma.prisma",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      }
    }
  }
}
